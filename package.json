{
  "name": "pi-package-mono",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo of personal pi coding-agent packages.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present"
  }
}
