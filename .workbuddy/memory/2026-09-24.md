# 2026-09-24

## 移除 pi-browser 包

判定 pi-browser 无存在必要，从 monorepo 删除。该包零 inbound 依赖（repo 约定 "no package depends on another package here"），删除无连带风险。

已改动：
- 删除 `packages/pi-browser/`（22 文件）
- `package-lock.json`：移除 `node_modules/@bytetrue/pi-browser` 与 `packages/pi-browser` 两处 workspace 条目（共 18 行），用 `npm install --package-lock-only` 验证过自洽
- `README.md`：Six→Five、删表格行 / install 示例 / `/browser` TUI 命令 / setup-only bullet / repository map 行
- `.pi/settings.json`：移除本地路径引用与 `npm:@bytetrue/pi-browser` 条目
- `.github/workflows/ci.yml`：删 `pack --dry-run` 步骤
- `.github/workflows/release.yml`：删 tag 注释、on.tags、case 分支

按用户要求 `byissue/` 文档**一行未动**（spec/pi-browser、epics/005、issues 074/075/081/083/085 均为 closed 历史，保留）。

npm 侧结论：`@bytetrue/pi-browser` 共 6 版（0.1.0→0.4.0），最新 0.4.0 发布于 2026-09-15，**超出 npm 72 小时撤回窗口**，命令行无法 unpublish。上周下载 92、零依赖方——符合申请彻底删除的条件，需走 npm support 工单；退而求其次是 `npm deprecate`。本机 npm 无 auth（发布走 GitHub Actions OIDC Trusted Publishing），需先 `npm login`。

## 环境要点

Git Bash 里 `npm`/`node` 不在 PATH。可用 mise shim 直接调：
`/c/Users/byte/AppData/Local/mise/shims/npm`（npm 11.17.0 / node v24.19.0）
